export const sortByQueryOption = {
  name: 'sortBy',
  enum: ['rating', 'usageFee', 'viewCount'],
  required: false,
};

export const sortOrderOption = {
  name: 'sortOrder',
  enum: ['asc', 'desc'],
  required: false,
};

export const pageOption = {
  name: 'page',
  required: false,
};

export const limitOption = {
  name: 'limit',
  required: false,
};

export const addressOption = {
  name: 'address',
  required: false,
};

export const usernameOption = {
  name: 'username',
  required: false,
};
